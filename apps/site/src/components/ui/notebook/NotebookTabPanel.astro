---
import CadTab from '@caderno-ui/astro/Tab.astro'

type Props = {
  class?: string
  id: string
}

const {
  class: className,
  id,
} = Astro.props
---

<CadTab class={className} id={id}>
  <slot />
</CadTab>
