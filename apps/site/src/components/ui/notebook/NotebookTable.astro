---
import '@/styles/notebook-table.css'

type TableCell = number | string
type TableColumn = {
  key: string
  label: string
}

type Props = {
  caption?: string
  class?: string
  columns: TableColumn[]
  rows: Record<string, TableCell>[]
  zebra?: boolean
}

const {
  caption,
  class: className,
  columns,
  rows,
  zebra = false,
}: Props = Astro.props
---

<div class:list={['notebook-table-wrap', className]} data-zebra={zebra ? 'true' : 'false'}>
  <table class="notebook-table">
    {caption ? <caption>{caption}</caption> : null}
    <thead>
      <tr>{columns.map((column) => <th scope="col">{column.label}</th>)}</tr>
    </thead>
    <tbody>
      {rows.map((row) => (
        <tr>{columns.map((column) => <td>{row[column.key]}</td>)}</tr>
      ))}
    </tbody>
  </table>
</div>
